{
  "name": "ripo-skills-main",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "prettier --check src/",
    "format": "prettier --write src/"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "prettier": "*",
    "typescript": "*"
  }
}
